---
title: GET /api/withdrawals
excerpt: >-
  Lists withdrawal requests for the authenticated user with status and metadata.


  **Authentication:** `Authorization: <API key>` — send the raw key (no `Bearer`
  prefix required). `Bearer <API key>` is still accepted for compatibility.
api:
  file: dce-api-openapi.yaml
  operationId: get_api_withdrawals
hidden: false
---